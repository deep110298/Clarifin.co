{
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/clarifin run build && cp -r artifacts/clarifin/dist/public public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
